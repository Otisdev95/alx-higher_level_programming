#!/usr/bin/node

class Rectangle {
    constructor(width, height) {
        if (w > 0 && h > 0) {
            this.w = width;
            this.h = height;
        }  
    }
}

module.exports = Rectangle;